### Running with dev profile which runs with H2 DB:`
`./gradlew bootRun --args='--spring.profiles.active=dev'`

### Running integration and unit tests

`gradle clean test`

### To run the spring boot server
`./gradlew bootRun`
